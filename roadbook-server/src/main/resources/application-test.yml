spring:
  datasource:
    url: jdbc:h2:mem:roadbook;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
app:
  jwt:
    secret: test-secret-key-not-for-production-123456789
  amap:
    key: test-key
